name: 'Changelog Cleaning'
description: 'Prints yayayay and checks out a destination repo'
inputs:
  local_file_path:
    description: 'Path to the local file to read'
    required: true
  destination_repo:
    description: 'Destination repository in owner/repo format'
    required: true
  destination_file_path:
    description: 'Relative path to the destination file within the repo'
    required: true
  checkout_directory:
    description: 'Directory to checkout the destination repository'
    required: true
runs:
  using: "composite"
  steps:
    - name: Checkout destination repository
      uses: actions/checkout@v4
      with:
        repository: ${{ inputs.destination_repo }}
        path: ${{ inputs.checkout_directory }}
    - name: Set up Rust toolchain
      uses: metalbear-co/setup-rust-toolchain@009cda47e1b529982a00627a40eda87b4215035a
    - name: Install rust-script
      run: cargo install rust-script --locked
      shell: bash
    - name: Run main.rs with rust-script
      run: |
        cat "${{ inputs.local_file_path }}" | \
        rust-script "${{ github.action_path }}/src/main.rs" > "${{ inputs.checkout_directory }}/${{ inputs.destination_file_path }}"
      shell: bash
    - name: Commit to destination repo
      working-directory: ${{ inputs.checkout_directory }}
      run: |
        git config --repo user.email "eyal@metalbear.co"
        git config --repo user.name "Eyal Bukchin"
        git add .
        git commit -m "Update changelog"
        git push
      shell: bash
    - name: Cleanup
      continue-on-error: true
      if: ${{ always() }}
      run: |
        rm -rf "${{ inputs.checkout_directory }}"
      shell: bash
branding:
  icon: 'terminal'
  color: 'purple'
